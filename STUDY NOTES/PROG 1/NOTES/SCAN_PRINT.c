
/*
Hello! , We will be learning about STDIO.H
It is a header file, a library is basically a collection of prebuilt functions from C, 
STDIO gives us Standard Input / Output functions.

(Learn more about Libraries on libraries notes).

This includes , 
printf("Hello!"); --- Prints.
scanf("%d",&Variable); --- Gets user input.



More about scanf, 

    - Requires a formate Specifier , (Visit Data_type notes).
    - Your data type must be similar to your input. 
    - Requires a Variable and "&" symbol, (Visit Variable Notes).
    more about the "&" , turns a variable into an address , and places said input to that address.
    (More on addresses notes ( Prog 2 Heavy ) )

For Example 
    scanf("%d",&Variable);

    %d  -> Integer (int)
    %f  -> Float (float)
    %lf -> Double / Long Float (double)
    %c  -> Character (char)
    %s  -> String / Text (char array)


*/


//First declare your Library.


int main(){


    return 0;
}



/*
Explaination for the code format,
{ } <- Represents the opening and closing of a block of code.

int main() <- Declaration of main (Main is where the code runs first).

return <- is a ending statement. (More about it on return notes).

*/ 