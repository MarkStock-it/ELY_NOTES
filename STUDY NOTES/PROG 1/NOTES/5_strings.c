/*
Hello!

What are strings?
Strings are a array of characters (more in array notes).

characters have integer values awswell, it's called ASCII Values,
This can be useful in different ways.

Miss Peña taught us this:

Every C string ends with a null terminator, written as '\0'.

A char array declared as:

    char String[50];

has 50 positions, with indexes 0 through 49.

When it contains a valid C string, it can hold at most 49 visible
characters. Those characters use indexes 0 through 48, while index 49
stores the null terminator '\0'.

(Goated Advice Btw haha)

*/

#include <stdio.h>
#include <stdbool.h>
/*
Make a function that takes input [Y / N].
To learn About how to differntiate values , go on (Conditionals note)
*/

bool Decider(char input){
    return input == 'Y';
}


int main (){
    

    char Input; // Takes one character.

    char String[50]; // An array of characters that takes over 50 character values.

    
    


}
