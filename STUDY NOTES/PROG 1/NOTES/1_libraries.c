/*
Hello! 

Header files (or libraries) are collections of pre-built functions in C!

Notice scanf("%d", &num)?
  - scanf  -> Function name
  - (...)  -> Parameters / Arguments passed into the function
  (Learn more in the Functions notes!)

Header files are included at the very top of your file using preprocessor 
directives (lines starting with #). 



Common Header Files Breakdown -------------------

#include <stdio.h>   -> Standard Input/Output (printf, scanf)
#include <stdlib.h>  -> Standard Library (memory allocation, conversion, system utilities)
#include <stdbool.h> -> Enables true/false Boolean types in C
#include <string.h>  -> String manipulation functions (strlen, strcpy, strcmp)
#include <math.h>    -> Math functions (pow, sqrt, abs)

-------------------------------------------------


// Code structure:

#include <stdio.h>
#include <stdlib.h>

int main() {

    // Your main code logic goes here!

    return 0;
}


For more in-depth knowledge about each header, search online or check documentation—they're widely documented!
*/